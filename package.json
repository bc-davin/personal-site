{
  "name": "personal-site",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build:prod",
    "vercel-build": "cd client && npm install && npm run build:prod"
  }
}
