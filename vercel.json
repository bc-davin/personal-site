{
  "version": 2,
  "buildCommand": "cd client && npm install && npm run build:prod",
  "outputDirectory": "client/dist/client-app",
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.py"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
